/// <reference path="utilityFunctions.ts"/>

const result1 = Utility.maxBoxAllowed(80);

console.log(result1);

import util = Utility.Fees;

const result2 = util.calculateFee(20);

console.log(result2);
